# SoundBlue Tools

> React + TypeScript + Vite 기반의 유틸리티 앱 모음 대시보드

## 개요

SoundBlue Tools는 다양한 유틸리티 도구들을 한 곳에서 접근할 수 있는 웹 대시보드입니다.

### 현재 제공되는 앱

| 아이콘 | 앱 이름 | 설명 | 경로 |
|:------:|--------|------|------|
| 📄 | 계약서 분석 도구 | Contract Risk Analysis | `/contract` |
| 🎵 | 메트로놈 | Metronome | `/metronome` |
| 📱 | QR 코드 생성기 | QR Code Generator | `/qr` |

---

## 프로젝트 구조

```
tools/
├── 📁 public/                     # 정적 파일
│   └── vite.svg                  # 파비콘
│
├── 📁 src/                        # 소스 코드
│   │
│   ├── 📁 apps/                  # 개별 앱 설정
│   │   ├── contract/config.ts    # 계약서 분석 도구 설정
│   │   ├── metronome/config.ts   # 메트로놈 설정
│   │   └── qr/config.ts          # QR 코드 생성기 설정
│   │
│   ├── 📁 assets/                # 에셋 파일
│   │   └── react.svg             # React 로고
│   │
│   ├── 📁 components/            # React 컴포넌트
│   │   ├── index.ts              # 배럴 export
│   │   ├── AppCard.tsx           # 개별 앱 카드 컴포넌트
│   │   ├── AppGrid.tsx           # 앱 그리드 레이아웃 컴포넌트
│   │   ├── AppList.tsx           # 앱 리스트 컴포넌트
│   │   ├── AppItem.tsx           # 앱 아이템 컴포넌트
│   │   ├── ErrorBoundary.tsx     # 에러 경계 컴포넌트
│   │   ├── 📁 layout/            # 레이아웃 컴포넌트
│   │   │   ├── Container.tsx     # 컨테이너
│   │   │   ├── Layout.tsx        # 기본 레이아웃
│   │   │   └── PageLayout.tsx    # 페이지 레이아웃
│   │   └── 📁 ui/                # UI 컴포넌트
│   │       ├── Button.tsx        # 버튼
│   │       ├── Input.tsx         # 입력 필드
│   │       ├── Select.tsx        # 셀렉트 박스
│   │       ├── Skeleton.tsx      # 스켈레톤 로더
│   │       └── Loader.tsx        # 로딩 스피너
│   │
│   ├── 📁 constants/             # 상수 정의
│   │   ├── index.ts              # 배럴 export
│   │   ├── apps.ts               # 앱 목록 데이터
│   │   └── sortOptions.ts        # 정렬 옵션
│   │
│   ├── 📁 hooks/                 # 커스텀 훅
│   │   ├── index.ts              # 배럴 export
│   │   ├── useSearch.ts          # 검색 훅
│   │   ├── useSort.ts            # 정렬 훅
│   │   ├── useDebounce.ts        # 디바운스 훅
│   │   ├── useLocalStorage.ts    # 로컬 스토리지 훅
│   │   └── useMediaQuery.ts      # 미디어 쿼리 훅
│   │
│   ├── 📁 pages/                 # 페이지 컴포넌트
│   │   ├── Home.tsx              # 홈 (대시보드)
│   │   ├── Contract.tsx          # 계약서 분석
│   │   ├── Metronome.tsx         # 메트로놈
│   │   ├── QR.tsx                # QR 코드 생성기
│   │   └── NotFound.tsx          # 404 페이지
│   │
│   ├── 📁 styles/                # 스타일 파일
│   │
│   ├── 📁 test/                  # 테스트 설정
│   │   ├── setup.ts              # 테스트 셋업
│   │   └── test-utils.tsx        # 테스트 유틸리티
│   │
│   ├── 📁 types/                 # TypeScript 타입 정의
│   │   ├── index.ts              # App, AppList 인터페이스
│   │   └── env.d.ts              # 환경 타입
│   │
│   ├── 📁 utils/                 # 유틸리티 함수
│   │   ├── index.ts              # 배럴 export
│   │   ├── cn.ts                 # 클래스네임 유틸리티
│   │   ├── format.ts             # 포맷 유틸리티
│   │   └── storage.ts            # 스토리지 유틸리티
│   │
│   ├── App.tsx                   # 루트 컴포넌트 (React Router)
│   ├── App.css                   # 앱 스타일
│   ├── index.css                 # 글로벌 스타일
│   ├── main.tsx                  # 엔트리 포인트
│   └── vite-env.d.ts             # Vite 타입 선언
│
├── index.html                     # HTML 템플릿 (SEO, 메타태그)
├── package.json                   # 의존성 및 스크립트
├── vite.config.ts                 # Vite 빌드 설정
├── vitest.config.ts               # Vitest 테스트 설정
├── tsconfig.json                  # TypeScript 설정
├── eslint.config.js               # ESLint 설정
├── .prettierrc                    # Prettier 설정
├── .prettierignore                # Prettier 제외 파일
└── wrangler.jsonc                 # Cloudflare Workers 배포 설정
```

---

## 작동 방식

### 애플리케이션 흐름도

```
┌─────────────────────────────────────────────────────────────────┐
│                         index.html                              │
│  • SEO 메타태그 (Open Graph, 설명, 키워드)                        │
│  • 성능 최적화 (DNS Prefetch, Preconnect, Critical CSS)          │
│  • 다크/라이트 테마 지원                                          │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                          main.tsx                               │
│            React 앱 마운트 (StrictMode로 감싸서 실행)              │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                           App.tsx                               │
│  • React.lazy()로 AppGrid 동적 로딩 (코드 스플리팅)               │
│  • Suspense + GridSkeleton으로 로딩 상태 처리                    │
│  • 개발 모드에서 성능 메트릭 모니터링                              │
└─────────────────────────────────────────────────────────────────┘
                                │
              ┌─────────────────┴─────────────────┐
              ▼                                   ▼
┌─────────────────────────┐         ┌─────────────────────────┐
│    constants/apps.ts    │         │      AppGrid.tsx        │
│                         │         │                         │
│  앱 목록 데이터 정의      │────────▶│  앱 카드들을 그리드로     │
│  (Object.freeze로 불변)  │         │  배치하는 컨테이너        │
└─────────────────────────┘         └─────────────────────────┘
                                              │
                                              ▼
                                    ┌─────────────────────────┐
                                    │      AppCard.tsx        │
                                    │                         │
                                    │  • 개별 앱 카드 렌더링    │
                                    │  • Hover시 URL prefetch │
                                    │  • 클릭시 해당 앱으로 이동 │
                                    └─────────────────────────┘
```

### 핵심 파일 상세 설명

| 파일 | 역할 | 주요 기능 |
|------|------|----------|
| `main.tsx` | 엔트리 포인트 | React 앱을 DOM에 마운트, StrictMode 적용 |
| `App.tsx` | 루트 컴포넌트 | Lazy Loading, Suspense, 성능 모니터링 |
| `AppGrid.tsx` | 그리드 레이아웃 | useMemo로 카드 목록 메모이제이션 |
| `AppCard.tsx` | 앱 카드 | Hover prefetch, 커스텀 memo 비교 함수 |
| `apps.ts` | 앱 데이터 | 불변 앱 목록 (id, name, desc, icon, url) |
| `types/index.ts` | 타입 정의 | App 인터페이스, AppList 타입 |

### 타입 구조

```typescript
// types/index.ts
interface App {
  readonly id: number;      // 고유 식별자
  readonly name: string;    // 앱 이름 (한국어)
  readonly desc: string;    // 앱 설명 (영어)
  readonly icon: string;    // 이모지 아이콘
  readonly url: string;     // 라우트 경로
}

type AppList = readonly App[];
```

---

## 성능 최적화

이 프로젝트에는 다양한 성능 최적화 기법이 적용되어 있습니다:

| 최적화 기법 | 적용 위치 | 설명 |
|------------|----------|------|
| **Code Splitting** | `App.tsx` | `React.lazy()`로 컴포넌트 동적 임포트 |
| **Memoization** | 모든 컴포넌트 | `memo()`, `useMemo()`, `useCallback()` |
| **CSS Containment** | `App.css` | `contain: content`로 레이아웃 격리 |
| **Prefetching** | `AppCard.tsx` | 마우스 hover 시 다음 페이지 미리 로드 |
| **Skeleton UI** | `App.tsx` | 로딩 중 스켈레톤 애니메이션 표시 |
| **Vendor Chunking** | `vite.config.ts` | React를 별도 청크로 분리 (캐싱 최적화) |
| **DNS Prefetch** | `index.html` | 외부 리소스 DNS 미리 조회 |
| **Critical CSS** | `index.html` | 초기 렌더링에 필요한 CSS 인라인 삽입 |
| **Reduced Motion** | `App.css` | 접근성을 위한 애니메이션 비활성화 지원 |

---

## 기술 스택

| 분류 | 기술 | 버전 |
|------|------|------|
| **프레임워크** | React | ^18.3.1 |
| **라우팅** | React Router | ^7.9.6 |
| **언어** | TypeScript | ^5.5.3 |
| **빌드 도구** | Vite | ^5.4.1 |
| **테스팅** | Vitest | ^2.1.8 |
| **테스팅** | React Testing Library | ^16.1.0 |
| **린팅** | ESLint | ^9.9.0 |
| **포맷팅** | Prettier | ^3.4.2 |
| **배포** | Cloudflare Pages | - |

---

## 시작하기

### 1. 의존성 설치
```bash
npm install
```

### 2. 개발 서버 실행
```bash
npm run dev
```
> 기본적으로 `http://localhost:5173`에서 실행됩니다.

### 3. 프로덕션 빌드
```bash
npm run build
```

### 4. 빌드 결과물 미리보기
```bash
npm run preview
```

### 5. 코드 린팅
```bash
npm run lint          # 린트 검사
npm run lint:fix      # 린트 자동 수정
```

### 6. 코드 포맷팅
```bash
npm run format        # Prettier로 포맷팅
npm run format:check  # 포맷 검사만
```

### 7. 테스트 실행
```bash
npm run test          # 테스트 watch 모드
npm run test:run      # 테스트 1회 실행
npm run test:coverage # 커버리지 리포트
npm run test:ui       # Vitest UI
```

### 8. 전체 검증
```bash
npm run validate      # 타입체크 + 린트 + 테스트
```

---

## 새 앱 추가하기

### Step 1: 앱 데이터 추가

`src/constants/apps.ts` 파일에 새 앱 정보를 추가합니다:

```typescript
export const APPS: AppList = Object.freeze([
  // ... 기존 앱들
  Object.freeze({
    id: 4,                          // 고유 ID (기존 최대값 + 1)
    name: '새로운 앱',               // 앱 이름 (한국어)
    desc: 'New App Description',    // 앱 설명 (영어)
    icon: '🆕',                     // 이모지 아이콘
    url: '/new-app'                 // 라우트 경로
  }),
]);
```

### Step 2: 라우트 구현

해당 URL 경로에 대한 페이지 컴포넌트를 구현합니다. (라우터 설정 필요)

---

## 스타일 가이드

### 테마 지원

- **다크 모드**: 기본 테마 (배경: `#242424`)
- **라이트 모드**: `prefers-color-scheme: light` 미디어 쿼리로 자동 전환

### CSS 변수

```css
:root {
  --card-bg: rgba(255, 255, 255, 0.1);
  --card-bg-hover: rgba(255, 255, 255, 0.2);
  --card-border: rgba(255, 255, 255, 0.1);
  --card-radius: 12px;
  --card-gap: 1rem;
  --card-padding: 1.5rem;
  --transition-fast: 150ms;
}
```

---

## 변경 로그 (Changelog)

### v1.0.0 (2024년)

아래는 프로젝트의 주요 업데이트 및 변경사항입니다.

| PR | 날짜 | 유형 | 변경 내용 |
|----|------|------|----------|
| #18 | 최신 | ⚡ perf | **SPA 성능 최적화** - 전체적인 Single Page Application 성능 개선 |
| #17 | - | 🔧 refactor | **상용급 패키지 리팩토링** - hooks, utils, components 구조 개선, 테스트 환경 구축 (Vitest), Prettier 추가 |
| #16 | - | 🐛 fix | **한글 IME 입력 버그 수정 (2차)** - 검색창 한글 조합 처리 추가 개선 |
| #15 | - | 🐛 fix | **한글 IME 입력 버그 수정** - 검색창에서 한글 입력 시 조합 중 오류 해결 |
| #14 | - | ✨ feat | **SPA 전환** - React Router 도입으로 페이지 전환 시 새로고침 없이 즉시 이동, Cloudflare Pages 캐시 헤더 최적화 |
| #13 | - | ✨ feat | **SEO 최적화** - tools.soundbluemusic.com canonical URL 및 og:url 메타태그 추가 |
| #12 | - | ✨ feat | **앱 정렬 기능** - 6가지 정렬 옵션 드롭다운 추가 (이름순, 최신순 등) |
| #11 | - | ✨ feat | **검색 기능 추가** - 앱 검색, 깔끔한 리스트 레이아웃, 프로덕션 성능 개선 |
| #10 | - | ✨ feat | **앱 자동 로드** - `src/apps/[name]/config.ts`에서 앱 설정 자동 로드 |
| #9 | - | 📝 docs | **프로젝트 문서화** - 한국어 README 작성 (구조, 사용법, 가이드) |
| #8 | - | ⚡ perf | **React 성능 최적화** - memo, useMemo, useCallback, 코드 스플리팅 적용 |
| #7 | - | 🔧 refactor | **프로젝트 이름 변경** - vite-react → tools |
| #2 | - | 🐛 fix | **빌드 오류 수정** - package-lock.json 추가 |

---

### 주요 기능 상세

#### ✨ 검색 및 정렬 (PR #11, #12)
- 실시간 앱 검색 기능
- 6가지 정렬 옵션: 이름(오름차순/내림차순), 최신순, 오래된순, 카테고리별

#### 🚀 SPA 전환 (PR #14)
- React Router v7 도입
- 페이지 전환 시 새로고침 없이 즉시 이동
- Cloudflare Pages 캐시 최적화

#### 🔧 상용급 리팩토링 (PR #17)
- **Custom Hooks**: useSearch, useSort, useDebounce, useLocalStorage, useMediaQuery
- **Utils**: cn (classnames), format, storage
- **Components**: UI 컴포넌트 분리 (Button, Input, Select, Skeleton, Loader)
- **Layout**: Container, Layout, PageLayout
- **Testing**: Vitest + React Testing Library 환경 구축
- **Formatting**: Prettier 설정 추가

#### ⚡ 성능 최적화 (PR #8, #18)
- React.lazy() 코드 스플리팅
- memo(), useMemo(), useCallback() 메모이제이션
- CSS Containment
- Vendor Chunking (React 별도 청크)
- DNS Prefetch, Critical CSS

---

## 라이선스

Private
