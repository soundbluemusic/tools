/**
 * Vanilla TypeScript App Entry Point
 * This is the main entry point for the vanilla (non-React) version of the app
 */

// Import styles
import '../index.css';

// Import and initialize the app
import './App';

// Log initialization
console.log('[Vanilla] App initialized');
