import type { AppConfig } from '../../types';

const config: AppConfig = {
  name: 'QR 코드 생성기',
  desc: 'QR Code Generator',
  icon: '📱',
  size: 153600, // 150KB
};

export default config;
