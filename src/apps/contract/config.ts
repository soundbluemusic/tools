import type { AppConfig } from '../../types';

const config: AppConfig = {
  name: '계약서 분석 도구',
  desc: 'Contract Risk Analysis',
  icon: '📄',
  size: 512000, // 500KB
};

export default config;
